import { TransitDamageInfoComponent } from './transit-damage-info.component'

describe('TransitDamageInfoComponent', () => {
  it('should mount', () => {
    cy.mount(TransitDamageInfoComponent)
  })
})